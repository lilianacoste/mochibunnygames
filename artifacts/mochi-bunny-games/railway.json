
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd artifacts/mochi-bunny-games && corepack enable && corepack prepare pnpm@latest --activate && pnpm install --no-frozen-lockfile && pnpm build"
  },
  "deploy": {
    "startCommand": "cd artifacts/mochi-bunny-games && pnpm preview --host 0.0.0.0 --port 8080"
  }
}
